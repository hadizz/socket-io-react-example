{
  "private": "true",
  "name": "socket-io-react-example",
  "version": "1.0.0",
  "license": "MIT",
  "workspaces": ["packages/*"],
  "scripts": {
    "client": "yarn workspace client start",
    "server": "yarn workspace server dev",
    "start": "concurrently yarn server yarn client"
  },
  "dependencies": {
    "concurrently": "^7.3.0"
  }
}
